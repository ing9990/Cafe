spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create

  datasource:
    url: jdbc:mysql://localhost:3306
    username: root
    password: 1111

server:
  port: 8080